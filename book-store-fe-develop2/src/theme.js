import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6b8e23', // 원하는 색상 코드로 변경
    },
    secondary: {
      main: '#8b4513',
    },
  },
});

export default theme;
